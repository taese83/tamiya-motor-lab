/**
 * no-permission(영구) 설정 경로 Collapse 콘텐츠 id —
 * MeasureFigures 콘텐츠와 MeasureActionDock [설정 방법 보기]의 aria-controls를 연결한다.
 * S1은 단일 인스턴스 화면이라 정적 id로 충분하다.
 */
export const S1_SETTINGS_HELP_ID = 'measure-settings-help'
